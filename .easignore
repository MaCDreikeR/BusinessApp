# EAS Build Ignore File
# Apenas arquivos temporários e caches desnecessários

# Dependências (serão reinstaladas no build)
node_modules/

# Cache local do Expo
.expo/
.expo-shared/

# Builds locais
android/build/
android/.gradle/
android/app/build/
ios/build/
ios/Pods/

# Temp files
*.log
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Git
.git/

# Editor
.vscode/
.idea/

# IMPORTANTE: NÃO ignorar:
# - android/ (pasta raiz - já commitada, não usamos CNG/Prebuild)
# - ios/ (pasta raiz - já commitada, não usamos CNG/Prebuild)
# - assets/ (necessário para builds)
# - Arquivos de configuração

# NOTA: O aviso do expo-doctor sobre CNG é esperado e pode ser ignorado.
# Este projeto usa configuração nativa tradicional (não CNG/Prebuild).
